#include<stdio.h>
int main()
{
	int ptr;
	
	 ptr = (int*)calloc(2,sizeof(int));
	 
	 if (ptr==NULL)
	 {
	 	printf(" memory not allocated...");
	 }
	 else
	 {
	 	printf(" memory not allocated...");
	 }
}
